# PM-SS v1.3 (Render + Supabase) Environment Variables
# ================================================

# Supabase Session Pooler URL (IPv4 compatible)
# Example:
# postgresql://postgres.<projectRef>:<PASSWORD>@aws-1-<region>.pooler.supabase.com:5432/postgres?sslmode=require
DATABASE_URL=

# JWT signing secret (generate a long random string)
JWT_SECRET=

# Optional: restrict browser origin (recommended)
# Example: https://pm-ss.onrender.com
CORS_ORIGIN=

# Optional: admin seed (only used if initDb seeds demo data)
# ADMIN_USERNAME=hradmin
# ADMIN_PASSWORD=Pmss@1234
